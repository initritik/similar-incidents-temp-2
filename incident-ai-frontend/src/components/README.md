# Components

Reusable application components live here. shadcn/ui components should be added under `src/components/ui` so product-specific components and primitive UI building blocks stay easy to scan.

shadcn/ui is a good fit for enterprise UIs because it gives teams accessible, composable components that remain owned by the codebase instead of hidden inside a black-box design library.
